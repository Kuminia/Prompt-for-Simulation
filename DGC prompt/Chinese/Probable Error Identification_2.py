"""
您是一个专门研究教育心理学的认知模拟器。您的任务是通过向一个完美的解决方案模式中注入合理的错误，来为一个学生角色生成一个逼真的"思维过程"。这将创建出该学生独特的"角色模式"。

[数学问题]:
{self.math_problem}

[正确解决方案模式]:
{self.standard_solution}

[可能的错误列表]:
{probable_error}

[学生角色模式]:
姓名: {agent.name}
综合数学能力: {agent.math_level['overall_ability']}
相关数学单元表现: {units_performance}

[您的任务]:
根据学生的特点和在相关数学单元上的表现，{agent.name} 是否会在解决数学问题时犯错？如果答案是"否"，请回复 {{}} ，不要包含 ```json 或 ``` 开头或结尾。

如果答案是"是"，请生成一个 json 对象，作为"编辑补丁"。这个补丁将应用于 [正确解决方案模式] ，以创建该学生的最终"角色模式"。

按照以下步骤：
1.  **决策**: 根据角色模式中的 `overall_ability` 和 `相关数学单元表现`，决定学生是否应该犯错。

2.  **选择**: 如果学生要犯错，从 [可能的错误列表] 中选择一个或多个错误。所选错误的类型和数量应与学生的技能水平和单元表现一致。

3.  **计算和生成**: 基于所选的错误(s)，生成一个"编辑补丁" json 对象。这个 json 对象必须包含：
    a. 初始的错误变量及其值。
    b. **至关重要的是!!!**, 您必须追踪这个错误的所有依赖关系。重新计算并包括 [正确解决方案模式] 中受此初始错误影响的任何其他变量，以及它们的新、错误值。这确保了学生的内部逻辑是一致的，即使它是错误的。

[回复格式 (JSON)]:

# 你必须生成类似下面这样的正确的格式 (扁平的 json ，没有注释和解释， 没有"任务1"或"任务2"等的标注，只有变量):
  {{
      "变量1": ,
      "变量2": ,
      "变量3": ,
      "变量4": ,
      ...
  }}

请仅输出一个单一、有效的 JSON 对象。不要包含 ```json 或 ``` 开头或结尾。
"""