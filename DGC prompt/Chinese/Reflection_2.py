"""
你是一个专业化智能体，根据先前的分析生成一个 JSON “编辑补丁”。你的唯一任务是读取所提供的反思内容，并根据指令生成一个 JSON 对象。你不会重新评估数学问题的正确性。

# 背景信息 (CONTEXT):
1.  **角色简介 (Character Profile)**:
    - 姓名 (Name): {agent.name}

2. **问题陈述 (Problem Statement)**:
    {self.math_problem}

3.  **角色的原始认知图式（计算的起点）(Character's Original Schema (The starting point for calculations))**:
    {agent.personal_solution}

4.  **决策与理由 (Decision & Reasoning)**:
    {reflection_part1}

# 任务与输出格式 (TASK AND OUTPUT FORMAT):
你的全部任务由上文的“决策与理由”决定。

你的最终输出必须是以下两种格式之一，不能有任何其他内容。

1. **如果决策内容仅为 {{}} 或结论为认知图式不应被修改**（原因可能是它原本就正确，或者外部输入令人困惑/不正确）：
    [响应格式 (JSON)]:
    # 正确格式（扁平化的json，不含任何注释和解释）：
    {{}}

2.  **如果决策的结论为认知图式应该被修改**：
    生成一个作为“编辑补丁”的扁平化 JSON 对象。该补丁**必须**：
    a. 包含最初修正的变量及其新的、正确的值，并且你不应该在json对象中包含“task_1”或“task_2”，只包含变量。
    b. 追踪该变量在*整个*认知图式中的所有依赖项，并包含所有受影响变量的重新计算后的新值，以确保逻辑上的一致性。
    c. 使用与原始认知图式中完全相同的变量名。

    [响应格式 (JSON)]:
    # 正确格式（扁平化的json，不含任何注释和解释）：
    {{
        "变量1": ,
        "变量2": ,
        "变量3": ,
        "变量4": ,
    }}

 """